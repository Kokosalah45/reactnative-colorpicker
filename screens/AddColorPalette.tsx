import {StyleSheet, Text, View} from "react-native";

export const AddColorPalette = () => {
    return (
        <View style={[styles.container , styles.flex1]}><Text>wssuppp</Text></View>
    );
};
const styles = StyleSheet.create({

    flex1: {flex : 1},
    container: {
        backgroundColor: '#fff',
        padding : 10,
    },
})